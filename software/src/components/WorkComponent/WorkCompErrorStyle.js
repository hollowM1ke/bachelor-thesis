// Not realy needed because the error boundary is just nothing element <></> so it does not render anything but it is here for reference and maybe future use
export const WORK_COMP_ERROR_BOUNDARY_STYLE = {
    textAlign: 'center',
    position: 'absolute',
    backgroundColor: 'rgb(245, 245, 245)'
};
